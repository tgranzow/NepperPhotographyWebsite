<div class="container">
    <h1 id=testimonial-title>Client Testimonials</h1>
    <table>
        <tbody>
            <% @testimonials.each do |testimonial| %>
        <tr>
          <td>
          <div class="testimonial-post"><%= testimonial.posts %></div>
      </td>
        </tr>
        <% end %>
      </tbody>
  </table>
</div>
