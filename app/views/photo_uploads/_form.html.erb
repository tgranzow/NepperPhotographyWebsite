<%= form_for(photo_upload) do |f| %>
  <% if photo_upload.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo_upload.errors.count, "error") %> prohibited this photo_upload from being saved:</h2>

      <ul>
      <% photo_upload.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
